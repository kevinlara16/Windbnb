const Cities = ({ el }) => {
    return (
            <p className="loading">
                 <span className="material-symbols-outlined">
                            location_on
                        </span>
                {el.city}
            </p>
                
    );
}

export default Cities;
